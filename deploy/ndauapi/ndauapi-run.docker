# Run stage. Nothing should exist here but the executables.
FROM alpine:3.8

# Add the ca certs so that we can do outbound connections to https sites (honeycomb)
RUN apk add --no-cache  ca-certificates

# only copy the binary artifacts we produced earlier
COPY --from=ndauapi-build /bin/ndauapi /bin/

# ndauapi listens here for its requests
EXPOSE 3030/TCP

ENTRYPOINT ["/bin/ndauapi"]
